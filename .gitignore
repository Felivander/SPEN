node_modules
dist
dist-ssr
.env
.env.local
.env.*.local
.vercel
.DS_Store
*.log

tsconfig.tsbuildinfo

# Local tooling state (launch.json is kept — it's useful to share)
.claude/settings.local.json

# Design references. Kept local: two of these are third-party Braun/Rams
# product images, so they don't belong in a public repo. Delete this line if
# you want them tracked.
inspo/

# Generated by `npm run icons` at build time
public/icon-192.png
public/icon-512.png
public/apple-touch-icon.png
public/icon.svg

# esbuild scratch from scripts/test-parser.mjs
bundle-parse.mjs
